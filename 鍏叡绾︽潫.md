# Carbon · 客户管理后台 — 公共约束（对外交接版）

> 版本：v0.5（新增 PILOT 试用状态 + EntityContractEvent 表）
> 范围：实体（Entity）、契约（Contract）、契约委托（Delegation）、角色与权限（Role / Permission）、用户（User）、MFA、实体-用户关系（EntityUser）、操作员邀请（Invite）、订单、设备、审计事件、通知。
> 受众：与本项目对接的其它系统（通知中心、客户自助门户、设备激活服务、订单/履约系统、计费/发票系统、IAM）。

---

## 0. 重要变更

### v0.4 → v0.5

1. **新增 PILOT 契约状态**（试用）。`EntityContract.status` 枚举增加 `PILOT`。ISO 契约创建时默认为 PILOT（6 个月试用 + 费用全部强制为 0）。其它类型默认 ACTIVE。
2. 新增 **EntityContractEvent** 表（§1.13），负责契约级结构化事件流（绑定 / 续期 / 转正 / 挂起 / 恢复 / 终止 / 条款变更）。原有的 `EntityContract.events` 嵌套数组以兼容方式保留，但以 EntityContractEvent 为权威源。
3. 新增 `ctr.convert` 权限点（PILOT → ACTIVE 转正专用）。
4. **EXPIRED 拓展到 PILOT**：PILOT 契约到期同样派生 EXPIRED。网关侧拒绝登录（与 SUSPENDED 并列）。仅 admin 可续期 / 转正 / 终止。
5. `entityStatus()` 派生规则更新：PILOT 计为 "有 active 契约"；LOCKED = 所有 live 契约 ∈ {SUSPENDED, EXPIRED}。
6. 新增 5 个通知 topic：`contract.pilot_started` / `pilot_extended` / `pilot_expiring` / `pilot_expired` / `pilot_converted`。
7. 试用提醒节奏：距到期 T-15 / T-7 / T-3 / T-1 各发一次。

### v0.3 → v0.4

1. **客户公司**统一为 **Entity**。NPT 自身也是一个 Entity（持有唯一一份 ADMIN 契约）。
2. **契约**升级为独立表 `EntityContract`，新增 `AUTHORIZING ENTITY ID`，区分 ADMIN 授权与 ISO 授权。`Acquirer` / `PayFac` 类型移除，新增 `DISTRIBUTOR` 类型。
3. 新增 **EntityContractDelegation**（契约委托表）。
4. 新增 **EntityRoleBlocklist**（角色黑名单表）。
5. 用户表彻底独立：`User` + `MFAInfo` + `EntityUserRelationship` 三张表。USER 不再持 `roleIds` / `authorizingType`。
6. 邀请独立成 **OperatorInvite** 表，token 默认有效期 **7 天**。
7. Entity **没有自己的状态字段**。客户的"状态"完全派生自其多份契约的状态，UI 需按契约级渲染。

---

## 1. 数据字典

### 1.1 Entity（实体 / 公司）

`Entity` 是统一概念：NPT、ISO、ISV、Merchant、Sub-ISO 全部都是 Entity 表的一行；区别仅在于它持有哪种契约。

| 字段 | 类型 | 必填 | 取值 / 说明 |
|---|---|---|---|
| `id` | string (`e-xxx` / `c-xxx`) | ✓ | 业务主键 |
| `name` | string | ✓ | 公司名 |
| `country` | ISO 3166-1 alpha-2 | ✓ | `US` / `CN` / `DE` 等 |
| `address` | string | – | 一行制完整地址 |
| `contactName` | string | – | 主要联系人姓名 |
| `phoneCountryCode` | string (E.164 prefix) | – | `+1` / `+86` |
| `phone` | string | – | 不含区号的本地号码 |
| `timezone` | IANA tz string | – | `America/New_York` / `Asia/Shanghai`（不要存 UTC offset） |
| `license` | string | – | 营业执照 / 许可证号 |
| `tags` | Array&lt;string&gt; | – | 业务标签，**最多 15 条** |
| `remark` | string | – | 内部备注 |
| `registeredAt` | ISO8601 | – | 后台注册时间（mock 派生字段，非正式模型） |
| `status` | enum | ✓ | `ONBOARDING` / `ACTIVE` — 创建后为 `ONBOARDING`；首位操作员完成注册后后端自动 flip 为 `ACTIVE`，不可退回 |

**Entity 状态是持久化字段。** 仅 `ONBOARDING` / `ACTIVE` 两态。合同状态是另一轴，走 `EntityContract.status`，多契约 → 多状态。实体本身不再有 LOCKED 状态—admin 要冻结走逐合同 SUSPEND。

**Tags 推荐分类**（不强制）：
- **行业**：`retail` / `hospitality` / `f&b` / `logistics` / `marine` / `apparel` / `industrial` / `software` / `automotive`
- **规模**：`smb` / `mid-market` / `enterprise` / `platform`
- **渠道**：`direct` / `via-iso` / `via-sub-iso` / `referral` / `partner`
- **生命周期**：`just-signed` / `strategic` / `watchlist` / `renewal-due` / `expansion`

### 1.2 EntityContract（契约关系）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string (`ec-xxx`) | ✓ | 主键 |
| `authorizedEntityId` | string | ✓ | 该契约赋能的 Entity |
| `authorizedContractType` | enum | ✓ | `ADMIN` / `ISO` / `ISV` / `MERCHANT` / `DISTRIBUTOR`（代码写死）|
| `authorizingEntityId` | string | ✓ | 授权方 Entity（详见下方约束） |
| `authorizedAt` | ISO8601 | ✓ | 授权时间戳 |
| `effectiveFrom` | ISO8601 | – | 生效时间 |
| `effectiveTo` | ISO8601 \| null | – | 结束时间；null = 无限制 |
| `terminatedByEntityId` | string \| null | – | 由谁终止的 |
| `terminatedAt` | ISO8601 \| null | – | 终止时间 |
| `status` | enum | ✓ | `PILOT` / `ACTIVE` / `SUSPENDED` / `TERMINATED`（参见下方"状态机"） |
| `entitlements` | JSONB | – | 按合同类型不同形态，见下 |
| `events` | JSONB | – | `Array<{ statusFrom, statusTo, description }>` — 契约级审计流（绑定/入驻/挂起/恢复/终止）。`statusFrom`/`statusTo` 可为 `null`。`description` 需自包含操作员 + 时间 |

**状态机（3 态 + 1 派生态）**

| 状态 | 含义 | 持久化？ |
|---|---|---|
| `PILOT` | **试用中**。所有费用条款锁定为 0，计费系统不开票。`effectiveFrom` = 创建当天，`effectiveTo` = +180d（不可手动改，只能走 Extend / Convert） | ✓ |
| `ACTIVE` | 契约在效，权限生效，起计费 | ✓ |
| `SUSPENDED` | admin 冻结，访问中止；契约仍在档，可恢复 | ✓ |
| `TERMINATED` | 契约已结束（软删）；写入 `terminatedAt`/`terminatedByEntityId`；不可恢复 | ✓ |
| `EXPIRED` *(derived)* | `(status == ACTIVE\|PILOT) && effectiveTo < now` — UI 在前端通过 `effectiveStatus()` 计算得出。**不写回 DB**。**网关侧拒绝操作员登录（与 SUSPENDED 并列）**。admin 可延长 `effectiveTo` 复活（若是 PILOT，走 Extend） | ✗ |

**状态转移**
- `PILOT → ACTIVE`（Convert，不可逆，需 `ctr.convert` 权限，需重填所有收费条款）
- `PILOT → TERMINATED`（终止合作）
- `PILOT` **不能直接 SUSPEND**（避免 Resume 后状态不明）
- `ACTIVE ↔ SUSPENDED`；`{ACTIVE, SUSPENDED} → TERMINATED`（单向）
- `EXPIRED` 不是真实存储状态，只是 `ACTIVE` / `PILOT` 的派生子态——所有筛选 / 派生统计 / Entity 状态推导都应基于 `effectiveStatus()` 而非 raw `status`。

**PILOT 行为细则**
- 创建：ISO 默认 PILOT；其它类型可手动指定 PILOT（数据模型支持），但默认 ACTIVE。`ADMIN` 类型禁止 PILOT。
- 续期 (Extend)：任何时候（含 EXPIRED 后）可由 admin 触发，`effectiveTo = today + N`（N ∈ {30, 60, 90}，不叠加在原 effectiveTo 上）。无次数上限。写 `PILOT_EXTEND` 事件。
- 转正 (Convert)：admin 主动操作，**不可逆**。重填全部收费条款（即使继续 0 也需显式输入）。`status: PILOT → ACTIVE`，`effectiveFrom = 转正当天`，计费起算日由 `effectiveFrom` 派生（当月 1 日 / 下月 1 日，看 effectiveFrom 是不是 1 日）。写 `PILOT_CONVERT` 事件。
- 计费：PILOT 期间不发任何账单。计费系统应按 contract.status 过滤掉 PILOT。
- 操作员邀请邮件：PILOT 期间发出的邀请应在邮件正文标注"试用阶段"。

> **历史注**：早先版本枚举里有 `PENDING`（旧的客户端签字流程残留）。当前模型契约由后台直接配置，`PENDING` 已移除。

**约束**
- 在 `status != TERMINATED` 状态下：`UNIQUE(authorizedEntityId + authorizedContractType + authorizingEntityId)`
- **`ADMIN` 契约全系统唯一**（系统内最多 1 条 `ACTIVE` ADMIN）。作为平台运营主体的 Entity 持有，自授权（`authorizingEntityId == authorizedEntityId`）。
- **`MERCHANT` 契约**：`authorizingEntityId` 必须是当前持有 `ACTIVE` `ISO` 契约的 Entity。ADMIN 不能直接发 MERCHANT 契约。
- **`DISTRIBUTOR` 契约**：只能由 ADMIN 授权。DISTRIBUTOR 为叶子节点，不能向下授权任何契约。
- **`ISO` / `ISV` 契约**：`authorizingEntityId` 既可以是 ADMIN 持有者，也可以是另一个 ISO（sub-ISO 模式）。**允许 ISO 互授**，不限制循环授权。

**Entitlements 形态**

只有 `ISO` 有 entitlements。其他类型（`ADMIN` / `ISV` / `MERCHANT` / `DISTRIBUTOR`）均为空 `{}`。

```ts
// ISO
entitlements: {
  deviceBasicService: { price: number, currency: string },                                   // 设备/月 基础费
  deviceModels: string[],                                                                     // 可售设备型号 ID 列表
  FlyDesk:        { enable: boolean, priceStrategy: { price: number, currency: string } },    // 远程桌面 · 月平摊
  GeoLocation:    { enable: boolean, priceStrategy: { price: number, currency: string } },    // 定位 · 单设备/月
  GeoFencing:     { enable: boolean, priceStrategy: { price: number, currency: string } },    // 电子围栏 · 1k 设备/月
  "Pre-warning":  { enable: boolean, priceStrategy: { price: number, currency: string } },    // 流量/电池/存储预警 · 月平摊
}

// 其他类型
entitlements: {}
```

> 计费单位：`deviceBasicService` / `GeoLocation` 按单设备；`GeoFencing` 按 1k 设备；`FlyDesk` / `Pre-warning` 月平摊（与设备数量无关）。

### 1.3 EntityContractDelegation（契约委托）

商户 M（由 ISO A 授权）的运营权，可被 A 委托给另一 ISO B。

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string (`del-xxx`) | ✓ | 主键 |
| `sourceContractId` | string (`ec-xxx`) | ✓ | 被委托的原始契约 ID（指向 EntityContract） |
| `delegatorEntityId` | string | ✓ | 委托方 Entity（= sourceContract.authorizingEntityId，冗余字段） |
| `delegateeEntityId` | string | ✓ | 被委托方 Entity（必须是 ISO） |
| `delegationScope` | enum | ✓ | `VIEW_ONLY`（仅查看商户信息与数据）/ `OPERATE`（日常运营；不含契约 / PROCESSOR ACCOUNT 操作） |
| `effectiveFrom` | ISO8601 | ✓ | 生效时间 |
| `effectiveTo` | ISO8601 \| null | – | 到期时间；null = 无限期 |
| `terminatedAt` | ISO8601 \| null | – | 实际终止时间 |
| `status` | enum | ✓ | `PENDING` / `ACTIVE` / `SUSPENDED` / `TERMINATED` |
| `createdByUserId` | string | ✓ | 发起委托的用户（必须属于 delegator 实体） |

**约束**
- `sourceContract` 必须是 `status = ACTIVE` 的 `MERCHANT` 类型契约
- `delegateeEntityId` 必须持有 `ACTIVE` `ISO` 契约（B 自己得是合法 ISO）
- `delegateeEntityId ≠ delegatorEntityId`（不能委托给自己）
- 在 `status != TERMINATED` 状态下：`UNIQUE(sourceContractId + delegateeEntityId)`
- **禁止再委托**：B 不能把从 A 处获得的委托再委托给 C

### 1.4 Role（角色）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | `r-xxx` | 主键 |
| `name` | string | 角色名 |
| `roleType` | enum | `global` / `private` |
| `contractDefineCode` | string \| null | 该角色归属的契约类型；null = 通用 |
| `entityId` | string \| null | `global` 时为 null；`private` 时必填，标识所属公司 |
| `permissions` | string[] | 权限 code 集合（详见 1.5；权限点系统预置不可新增） |
| `remark` | string | 描述 |

**应用层约束**
- `roleType = global` → `entityId` 必须为 null
- `roleType = private` → `entityId` 必须非空
- 当 `EntityUserRelationship` 引用某个 `private` Role 时，关系的 `entityId` 必须等于 `Role.entityId`

### 1.5 Permission（权限点；系统预置，不可新增）

权限 ID 命名空间：

| 命名空间 | 权限 ID |
|---|---|
| 客户管理 `cust.*` | `cust.view`、`cust.edit`、`cust.create`、`cust.lock`（contract 级 Suspend/Resume）、`cust.delete` |
| 契约 `ctr.*` | `ctr.view`、`ctr.add`、`ctr.sign`、`ctr.convert`、`ctr.terminate`、`ctr.delegate` |
| 操作员 `op.*` | `op.view`、`op.invite`、`op.role`、`op.lock`、`op.mfa`、`op.remove` |
| 敏感数据 `data.*` / `audit.*` | `data.reveal`、`data.export`、`audit.view` |
| 系统 `sys.*`（ADMIN 专属） | `sys.users.view/create/edit/lock/resetpw`、`sys.roles.view/edit`、`sys.notify`、`sys.keys` |
| ISV 专属 `isv.*` | `isv.apikey.view/rotate`、`isv.webhook.config`、`isv.sandbox.access`、`isv.app.publish`、`isv.app.metrics` |
| ISO 专属 `iso.*` | `iso.merchant.onboard/suspend/fee.edit`、`iso.residual.view/export` |
| Merchant 专属 `merchant.*` | `merchant.txn.view/refund/void`、`merchant.settle.view`、`merchant.dispute.respond` |

**实际生效作用域**：菜单（`MENU_TREE[n].contractDefineCode`）决定权限点的作用域。`null = 通用`；其他值 = 仅当 Role.contractDefineCode 匹配时该权限生效。

### 1.6 EntityRoleBlocklist（角色黑名单）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 主键 |
| `entityId` | string | 屏蔽方 Entity |
| `contractType` | enum | 屏蔽生效的契约类型（与 Role.contractDefineCode 对应） |
| `roleId` | string | 被屏蔽的 Role（一般是 global 角色） |

**约束**：`UNIQUE(entityId + contractType + roleId)`

### 1.7 User（用户，包含平台员工 + 客户操作员）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | `u-xxx` | ✓ | 主键 |
| `username` | string | ✓ | 登录用户名 |
| `passwordHash` | string | ✓ | 含 salt 的哈希值（明文不存储） |
| `passwordChangedTimestamp` | ISO8601 | – | 最近一次改密时间 |
| `passwordErrorTimes` | int | ✓ | 默认 0 |
| `passwordErrorLockExpiredTimestamp` | ISO8601 \| null | – | 错误次数触发锁定的自动解锁时点 |
| `nickname` | string | ✓ | 昵称 |
| `email` | email | ✓ | – |
| `country` | string | – | ISO 3166-1 alpha-2 |
| `mfaEnable` | bool | ✓ | 默认 false |
| `status` | enum | ✓ | `ACTIVE` / `LOCKED`（无 `PENDING`：未接受邀请仅存在于 `OperatorInvite` 表） |
| `lastLoginAt` | ISO8601 \| null | – | – |
| `passwordHistory` | JSON Array | – | 最多保留 **5 次** 旧密码哈希 |
| `remark` | string | – | – |

> **重要**：USER 表里**不再含 `roleIds` / `authorizingType`**。这些是 EntityUserRelationship 的字段。

### 1.8 MFAInfo

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string | ✓ | 主键 |
| `userId` | `u-xxx` | ✓ | – |
| `mfaType` | string | ✓ | 默认 `TOTP` |
| `secretEncrypted` | string | ✓ | 加密存储的 TOTP 密钥 |
| `failTimes` | int | – | 失败次数；> 10 触发 60 分钟锁 |
| `lastFailTimestamp` | ISO8601 \| null | – | 最后一次失败时间 |
| `status` | int | ✓ | `1` = ENABLE，`0` = DISABLE，`2` = PENDING_ACTIVE |

### 1.9 EntityUserRelationship（实体 ↔ 用户关系）

一名 User 可隶属多个 Entity，每条隶属关系单独一行。

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string | ✓ | 主键 |
| `entityId` | string | ✓ | – |
| `userId` | string | ✓ | – |
| `authorizingType` | enum | ✓ | `ADMIN`（拥有该 Entity 下所有权限）/ `NORMAL`（按 ROLE 配置） |
| `roleIds` | string[] | – | 仅 `authorizingType = NORMAL` 时有意义；支持多角色 |
| `authorizingTimestamp` | ISO8601 | ✓ | 创建时间 |
| `authorizingUserId` | string | ✓ | 授权人 User ID |
| `authorizingUserName` | string | – | 冗余字段便于展示 |
| `status` | enum | ✓ | `ACTIVE` / `EXPIRED` / `LOCKED`（无 `PENDING`） |
| `authorizingFrom` | ISO8601 \| null | – | 授权开始时间；null = 无限制 |
| `authorizingTo` | ISO8601 \| null | – | 授权结束时间，超过自动失效；null = 无限制 |

### 1.10 OperatorInvite（邀请）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string | ✓ | 主键 |
| `inviterUserId` | string | ✓ | 邀请者 User ID |
| `inviteEmail` | email \| null | – | 邮件邀请时必填；Copy link 模式可为空 |
| `token` | string | ✓ | 核心 TOKEN；URL 中携带 |
| `expiresAt` | ISO8601 | ✓ | 默认 createdAt + **7 天** |
| `consumedAt` | ISO8601 \| null | – | 接受时间；null = 未消费 |
| `resendCount` | int | – | 重发次数 |

> Mock 数据中扩展了 `entityId` / `inviterEntityId` / `inviterUserName` / `inviteRoleIds` / `inviteAuthType` / `inviteMethod` / `createdAt` 等辅助字段，便于 UI 展示。这些字段不属于正式模型，下游接入时按需取舍。

### 1.11 订单 Order / 行项 OrderItem / 设备 Device

（**与 v0.3 一致**，未改动；保留 `o-YYYY-NNNN` / `SO-YYYY-NNNN` 命名空间、订单状态机、6 位激活码 5 分钟有效、整单折扣 0–100% 等规则。详细字段见 v0.3。）

### 1.12 审计事件 Event

通用结构：`{ at: ISO8601, kind: string, by: email|"system", text: string }`

`kind` 取值：
- 实体域：`created`、`contract+`、`contract✓`、`contract-`、`operator+`、`info`、`reveal`、`warn`
- 契约域：`contract.suspend`、`contract.resume`、`contract.terminate`
- 委托域：`delegation.create`、`delegation.terminate`
- 订单域：`created`、`invoice`、`paid`、`ship`、`activate`、`deliver`、`free`

### 1.13 EntityContractEvent（契约事件流）

每个契约生命周期的结构化事件记录。与 `EntityContract.events` 嵌套数组并存（后者作为轻量级 timeline 冲击缓存，本表是权威源）。UI 从本表读取派生信息（试用启动日、续期次数、转正时间等）。

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string (`ece-xxx`) | ✓ | 主键 |
| `contractId` | string (`ec-xxx`) | ✓ | 外键 → EntityContract.id |
| `eventType` | enum | ✓ | 见下枚举 |
| `eventInfo` | JSONB | – | 按 eventType 不同形态，见载荷表 |
| `eventTimestamp` | ISO8601 | ✓ | 事件发生时间 |
| `operatorUserId` | string | ✓ | 操作人 User.id，或 `system`（调度器自动事件） |

**eventType 枚举**

| eventType | 起因 | 伴随数据变化 |
|---|---|---|
| `BIND_PILOT` | 创建 PILOT 契约 | INSERT EntityContract status='PILOT' |
| `BIND_ACTIVE` | 创建 ACTIVE 契约（直签） | INSERT EntityContract status='ACTIVE' |
| `PILOT_EXTEND` | 试用续期 | UPDATE EntityContract.effectiveTo |
| `PILOT_CONVERT` | 试用转正 | UPDATE status='ACTIVE' + entitlements + effectiveFrom |
| `SUSPEND` | 挂起 | UPDATE status='SUSPENDED' |
| `RESUME` | 恢复 | UPDATE status='ACTIVE' |
| `TERMINATE` | 终止合作 | UPDATE status='TERMINATED' + terminatedAt |
| `ENTITLEMENTS_EDIT` | 条款修改（不含状态变更） | UPDATE entitlements |

**eventInfo 载荷（按 eventType）**

| eventType | eventInfo 字段 |
|---|---|
| `BIND_PILOT` | `{ initialEffectiveTo, entitlementsSnapshot }` |
| `BIND_ACTIVE` | `{ effectiveFrom, effectiveTo?, entitlementsSnapshot }` |
| `PILOT_EXTEND` | `{ fromEffectiveTo, toEffectiveTo, days: 30\|60\|90, reason? }` |
| `PILOT_CONVERT` | `{ pilotStartedAt, pilotEntitlementsSnapshot, newEffectiveFrom, newEntitlementsSnapshot, derivedBillingStartsAt }` |
| `SUSPEND` | `{ reason }` |
| `RESUME` | `{}` |
| `TERMINATE` | `{ reason }` |
| `ENTITLEMENTS_EDIT` | `{ diff: { key: { from, to } }, contractStatusAtTheTime }` |

**派生查询（不持久化，运行时计算）**
- "是否曾经是试用" = `events.any(e.eventType == 'BIND_PILOT')`
- "续期次数" = `events.count(e.eventType == 'PILOT_EXTEND')`
- "总续期天数" = `sum(events.filter(...).eventInfo.days)`
- "转正时间" = `events.find(e.eventType == 'PILOT_CONVERT')?.eventTimestamp`

**与全局 AuditEvent（§4.3）的关系**
- `EntityContractEvent` — 契约本地时间线（Contract Activity 卡读这里）
- `AuditEvent` — 跨 scope 的总账（Audit Logs 页用），由 `EntityContractEvent` 衰生写入，`scope='contract'`，`scopeId=contract.id`

---

## 2. 业务规则

### 2.1 实体生命周期（无独立状态，由契约派生）

- Entity 创建后不自动激活——必须依附于至少一条契约才有业务能力。
- **客户列表 / 详情中的"状态"列**展示各份契约的状态 chip：`[ISO ●PILOT][ISV ●ACTIVE][MERCHANT ✕TERMINATED]`。
- 旧的"Onboarding/Active/Locked"概念派生规则：
  - **派生 Onboarding** = 所有 live 契约状态均为 PENDING（现代模型中不存在），或存在主合同但没有任何已激活的操作员
  - **派生 Active** = 至少一个有效 PILOT 或 ACTIVE 契约 + 至少一名操作员（PILOT 客户在产品层面与 ACTIVE 客户不区分）
  - **派生 Locked** = 所有 live 契约状态 ∈ {SUSPENDED, EXPIRED}（两者都会被网关拒绝登录）

### 2.1.1 试用生命周期（PILOT）

- **创建**：ISO 默认 PILOT（6 个月试用，费用锁定）；ISV / MERCHANT / DISTRIBUTOR 默认 ACTIVE，但数据模型支持它们走 PILOT。`ADMIN` 不可 PILOT。
- **续期**：`ctr.add` 权限。+30/+60/+90 天三选项，默认 +30。从今天起算，不叠加。任何时候（含 EXPIRED）都可续。无次数上限。写 PILOT_EXTEND 事件。
- **转正**：`ctr.convert` 权限。三步向导（警示 + 重填费用 + diff 确认）。不可逆。转正后 `status = ACTIVE`，`effectiveFrom = 今天`，计费起算日由 effectiveFrom 派生。写 PILOT_CONVERT 事件。
- **报表**：UI 可从 EntityContractEvent 拉出续期次数、转正转化率等指标。
- **跨合同限制**：PILOT 计为 live（同 UNIQUE 约束适用）：同一 entity 同类型不能同时有 PILOT + ACTIVE。
- **提醒**：距到期 T-15 / T-7 / T-3 / T-1 都发一次（§3.1）。
- **过期后行为**：不自动 TERMINATED。保持 EXPIRED 派生态，网关拒绝登录。admin 可续期 / 转正 / 终止。

### 2.2 契约
- 由后台直接配置（取代旧的客户端签署流程）。
- 状态机（§1.2）：PILOT / ACTIVE / SUSPENDED / TERMINATED + 派生 EXPIRED。
- 创建时 ISO 默认 PILOT，其他类型默认 ACTIVE。
- 转正（`PILOT → ACTIVE`）不可逆，需 `ctr.convert` + 重填所有收费条款。
- 终止契约必须填写理由（写入审计）。
- 修改契约 entitlements 写 `ENTITLEMENTS_EDIT` 事件 + `info` 审计。

### 2.3 契约委托
- 必须 `sourceContract.status = ACTIVE` 且类型为 `MERCHANT` 才能委托。
- delegatee 必须持有 `ACTIVE` `ISO` 契约。
- `OPERATE` 范围允许：商户/门店/终端日常维护、Ticket 处理、交易流水查看；**不含**：契约修改、PROCESSOR ACCOUNT 操作。
- `VIEW_ONLY` 范围仅允许查看商户信息与数据。
- 禁止再委托。

### 2.4 操作员 & 角色 & 邀请
- 角色由 `EntityUserRelationship.roleIds[]` 引用（支持多角色，权限合并）。
- `authorizingType = ADMIN` 的 EntityUser 拥有所属 Entity 下所有权限，**忽略 roleIds**。
- 新操作员通过**邀请链接 / QR 码**接入。邀请未消费前数据**只在 OperatorInvite 表**，**不创建 User**。
- 邀请被消费时：① INSERT User ② INSERT EntityUserRelationship ③ UPDATE OperatorInvite.consumedAt。
- TOKEN 默认 **7 天有效**。

### 2.5 角色黑名单
- 某 Entity 可在特定契约类型下屏蔽某些 global Role，使其无法被该 Entity 的 EntityUserRelationship 引用。

### 2.6 敏感信息与脱敏
- 邮箱、电话、姓名默认按 EO 14117 / GDPR 规则脱敏显示。
- 解除脱敏（`data.reveal`）**必须写审计**：`{ kind: 'reveal', text: 'Sensitive field revealed: <field>' }`。
- 导出 CSV 受 `data.export` 控制；默认脱敏导出。

### 2.7 订单与设备
（v0.3 规则不变。略。）

### 2.8 金额计算
- `subtotal = Σ unitPrice × qty`
- `total = round(subtotal × (1 − discountPct/100), 2)`
- 货币固定 USD；前端用 `en-US` `Intl.NumberFormat`。

---

## 3. 与外部系统的关系

### 3.1 通知 / 消息

| 触发事件 | 触发方 | 建议通道 | 收件人 | 负载关键字段 |
|---|---|---|---|---|
| Entity 注册完成 | 客户管理后台 | 站内信 + Email | 全部 ADMIN-EntityUser、Carbon 运营 | `entityId`、`name`、`registeredAt` |
| 契约新增 / 终止 / 状态切换 | 客户管理后台 | 站内信 + Email | 该 Entity 的 ADMIN-EntityUser | `entityContractId`、`type`、`status`、`actor` |
| **试用启动** (`contract.pilot_started`) | 客户管理后台 | 站内信 + Email | 该 Entity 的 ADMIN-EntityUser 、 Carbon 运营 | `entityContractId`、`initialEffectiveTo`、`actor` |
| **试用续期** (`contract.pilot_extended`) | 客户管理后台 | 站内信 | 该 Entity 的 ADMIN-EntityUser | `entityContractId`、`days`、`newEffectiveTo`、`reason`、`actor` |
| **试用即将过期** (`contract.pilot_expiring`) | 调度器（T-15/T-7/T-3/T-1） | Email + 站内信 | 该 Entity 的 ADMIN-EntityUser、Carbon 运营 | `entityContractId`、`daysLeft`、`effectiveTo` |
| **试用已过期** (`contract.pilot_expired`) | 调度器 | Email + 站内信 | 该 Entity 的 ADMIN-EntityUser、Carbon 运营 | `entityContractId`、`expiredAt` |
| **试用转正** (`contract.pilot_converted`) | 客户管理后台 | Email + 站内信 | 该 Entity 的 ADMIN-EntityUser、Carbon 运营 | `entityContractId`、`newEffectiveFrom`、`derivedBillingStartsAt`、`actor` |
| 契约委托创建 / 终止 | 客户管理后台 | 站内信 + Email | delegator 与 delegatee 双方 ADMIN | `delegationId`、`sourceContractId`、`scope`、`actor` |
| 操作员邀请 | 客户管理后台 | **Email + 邀请链接 + QR** | `inviteEmail` | `inviteId`、`token`、`entityId`、`roleIds`、`expiresAt` |
| 操作员锁定 / MFA 重置 | 客户管理后台 | Email | 被操作的 User + 所属 Entity 的 ADMIN | `userId`、`action`、`actor` |
| 敏感信息被解码（`data.reveal`） | 客户管理后台 | 站内信（仅 Compliance 角色） | 含 `data.reveal` 或 `audit.view` 的角色成员 | `entityId`、`field`、`actor`、`at` |
| 发票开具 | 计费/发票系统 | Email + 站内信 | 下单 Entity 的 ADMIN、`createdBy` | `orderId`、`invoiceNo`、`amount` |
| 支付到账 | 计费/支付系统（**回调**） | 站内信 | `createdBy`、Carbon 运营 | `orderId`、`paidAt`、`amount` |
| 订单发货 / 部分发货 | 履约/仓储系统（**回调**） | Email + 站内信 | 收货联系人、Entity ADMIN | `orderId`、`tracking`、`shippedItems` |
| 设备激活 | 设备/激活服务（**回调**） | 站内信 | `createdBy` | `orderId`、`sn`、`code`、`activatedAt` |

**通道配置**：本系统提供 `sys.notify` 权限，支持配置 email / webhook 路由，下游通知中心可通过 webhook 订阅以上事件。

### 3.2 与其它系统的接口约定（建议）
| 上游/下游 | 方向 | 接口语义 |
|---|---|---|
| **IAM / 身份服务** | ← | User 登录、MFA、锁账、邀请确认 |
| **计费 / 发票系统** | ↔ | 订单创建 → 发出 `invoice`；支付到账回调 → `paid` 事件 |
| **履约 / 仓储系统** | ↔ | 订单 `Awaiting shipment` 推送到仓储；仓储回写 `ship` / `deliver` / `tracking` |
| **设备激活服务** | ← | 设备首次开机上报 `(sn, code)` 对，匹配订单行项后回写 |
| **通知中心** | ← | 订阅上述事件，按收件人 + 通道分发 |
| **客户自助门户**（若存在） | ↔ | 读取脱敏后的合同、订单、设备列表；不持有 `data.reveal` 能力 |
| **审计 / SIEM** | ← | 拉取审计事件流 |

### 3.3 公共契约要点
- **跨系统事件统一携带 `{at, kind, by, text}` 形式的审计字段**。
- **邮箱/电话在跨系统传递时默认明文**，调用方需自行实现 UI 层脱敏（参考 §2.6）。
- 操作主体 `by` 取值：员工邮箱 / `admin@carbon` / `system` / `warehouse@carbon` 等内部 service account。

---

## 4. 建议的数据结构（对外接口形态）

### 4.1 通用约定
- 时间一律 ISO8601 + UTC（`…Z`）。
- 金额：`{ amount: integer, currency: "USD" }`（最小货币单位，分）。
- 枚举值在 wire 上使用 §1 中的英文字面量（与代码字面量一致）。
- 主键命名空间：`e-` / `c-` Entity、`ec-` EntityContract、`ece-` EntityContractEvent、`del-` Delegation、`r-` Role、`u-` User、`mfa-` MFAInfo、`eur-` EntityUserRelationship、`inv-` OperatorInvite、`o-` 订单、`m-` 设备型号、`li-` 行项。

### 4.2 实体 JSON 范型（TypeScript 风格）

```ts
type Email = string;
type ISODateTime = string;
type ContractType = 'ADMIN' | 'ISO' | 'ISV' | 'MERCHANT' | 'DISTRIBUTOR';
type ContractStatus = 'PILOT' | 'ACTIVE' | 'SUSPENDED' | 'TERMINATED';
type EntityContractEventType =
  | 'BIND_PILOT' | 'BIND_ACTIVE'
  | 'PILOT_EXTEND' | 'PILOT_CONVERT'
  | 'SUSPEND' | 'RESUME'
  | 'TERMINATE' | 'ENTITLEMENTS_EDIT';
type DelegationScope = 'VIEW_ONLY' | 'OPERATE';
type UserStatus = 'ACTIVE' | 'LOCKED';
type EurStatus = 'ACTIVE' | 'EXPIRED' | 'LOCKED';

interface Money { amount: number; currency: 'USD'; }

interface Entity {
  id: string;
  name: string;
  country: string;
  address?: string;
  contactName?: string;
  phoneCountryCode?: string;
  phone?: string;
  timezone?: string;            // IANA tz
  license?: string;
  tags: string[];               // ≤ 15
  remark?: string;
}

interface EntityContract {
  id: string;
  authorizedEntityId: string;
  authorizedContractType: ContractType;
  authorizingEntityId: string;
  authorizedAt: ISODateTime;
  effectiveFrom?: ISODateTime;
  effectiveTo?: ISODateTime;
  terminatedByEntityId?: string;
  terminatedAt?: ISODateTime;
  status: ContractStatus;
  entitlements: Record<string, unknown>;   // shape varies — see §1.2
}

interface EntityContractEvent {
  id: string;                              // 'ece-xxx'
  contractId: string;                      // → EntityContract.id
  eventType: EntityContractEventType;
  eventInfo: Record<string, unknown>;      // payload varies — see §1.13
  eventTimestamp: ISODateTime;
  operatorUserId: string;                  // User.id or 'system'
}

interface EntityContractDelegation {
  id: string;
  sourceContractId: string;
  delegatorEntityId: string;
  delegateeEntityId: string;
  delegationScope: DelegationScope;
  effectiveFrom: ISODateTime;
  effectiveTo?: ISODateTime;
  terminatedAt?: ISODateTime;
  status: ContractStatus;
  createdByUserId: string;
}

interface Role {
  id: string;
  name: string;
  roleType: 'global' | 'private';
  contractDefineCode: ContractType | null;
  entityId: string | null;       // null when global
  permissions: string[];
  remark?: string;
}

interface EntityRoleBlocklist {
  id: string;
  entityId: string;
  contractType: ContractType;
  roleId: string;
}

interface User {
  id: string;
  username: string;
  passwordHash: string;
  passwordChangedTimestamp?: ISODateTime;
  passwordErrorTimes: number;
  passwordErrorLockExpiredTimestamp?: ISODateTime;
  nickname: string;
  email: Email;
  country?: string;
  mfaEnable: boolean;
  status: UserStatus;
  lastLoginAt?: ISODateTime;
  passwordHistory: { id: string; changedTimestamp: ISODateTime; cretimeTimestamp: ISODateTime }[];   // ≤ 5
  remark?: string;
}

interface MFAInfo {
  id: string;
  userId: string;
  mfaType: 'TOTP';
  secretEncrypted: string;
  failTimes: number;
  lastFailTimestamp?: ISODateTime;
  status: 0 | 1 | 2;             // 0 DISABLE, 1 ENABLE, 2 PENDING_ACTIVE
}

interface EntityUserRelationship {
  id: string;
  entityId: string;
  userId: string;
  authorizingType: 'ADMIN' | 'NORMAL';
  roleIds: string[];
  authorizingTimestamp: ISODateTime;
  authorizingUserId: string;
  authorizingUserName?: string;
  status: EurStatus;
  authorizingFrom?: ISODateTime;
  authorizingTo?: ISODateTime;
}

interface OperatorInvite {
  id: string;
  inviterUserId: string;
  inviteEmail?: Email;
  token: string;
  expiresAt: ISODateTime;
  consumedAt?: ISODateTime;
  resendCount: number;
  // mock-only extras (entityId, inviteRoleIds, inviteAuthType, inviteMethod, createdAt)
  // are not part of the formal model — downstream may use or ignore.
}
```

### 4.3 审计事件（独立资源）

```ts
interface AuditEvent {
  id: string;
  scope: 'entity' | 'contract' | 'delegation' | 'role' | 'user' | 'order' | 'system';
  scopeId: string;
  at: ISODateTime;
  by: Email | 'system';
  kind: string;
  text: string;
  meta?: Record<string, unknown>;
}
```

### 4.4 通知事件（推送给通知中心 / Webhook）

```ts
interface NotificationEvent {
  id: string;
  topic:
    | 'entity.registered'
    | 'contract.added' | 'contract.status_changed' | 'contract.terminated'
    | 'contract.pilot_started' | 'contract.pilot_extended' | 'contract.pilot_expiring' | 'contract.pilot_expired' | 'contract.pilot_converted'
    | 'delegation.created' | 'delegation.terminated'
    | 'operator.invited' | 'operator.locked' | 'operator.mfa_reset'
    | 'data.revealed'
    | 'order.invoiced' | 'order.paid' | 'order.shipped' | 'order.delivered'
    | 'device.activated';
  occurredAt: ISODateTime;
  actor: Email | 'system';
  payload: Record<string, unknown>;
  audience: {
    entityId?: string;
    roleIds?: string[];
    emails?: Email[];
    channels: Array<'inapp' | 'email' | 'webhook'>;
  };
}
```

### 4.5 建议端点（REST 风格）
```
GET    /entities?contractType=&contractStatus=&q=&page=
GET    /entities/{id}
PATCH  /entities/{id}
POST   /entities/{id}/contracts                   # 新增契约
PATCH  /contracts/{id}                            # 改 status / entitlements
DELETE /contracts/{id}                            # 软删（→ TERMINATED）
POST   /contracts/{id}/delegations
DELETE /delegations/{id}

GET    /entities/{id}/operators                   # = EntityUserRelationship.list
POST   /entities/{id}/operators/invite            # → OperatorInvite
PATCH  /operator-invites/{id}/resend
DELETE /operator-invites/{id}                     # revoke
POST   /operator-invites/{id}/consume             # accept

GET    /roles?contractType=&entityId=
PATCH  /roles/{id}

GET    /users?status=
PATCH  /users/{id}                                # lock / unlock / resetpw

GET    /orders?entityId=&status=
… (订单系列接口与 v0.3 一致)

GET    /audit?scope=&scopeId=&from=&to=
POST   /webhooks/subscriptions                    # sys.notify 权限
```

---

## 5. 给到下游项目的最小信息清单

做通知中心 / 客户门户 / 履约对接时，**至少需要本系统提供**：
1. §1 全套枚举值（合同类型 4 种、合同状态 4 种、用户状态 2 种、关系状态 3 种、权限 ID 全集、事件 kind 全集）。
2. §3.1 完整事件清单及收件人规则。
3. §4.4 `NotificationEvent` schema 与示例载荷。
4. Webhook 鉴权方式（建议 HMAC + 时间戳）与重试 / 幂等策略（建议至少送达 + `event.id` 幂等键）。
5. 脱敏字段清单与"是否需要 reveal 权限"。
